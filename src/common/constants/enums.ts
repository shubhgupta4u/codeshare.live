export enum Theme {
    Light = 1,
    Dark,
    DarkPlus,
    LightPlus
  }
  export enum MenuItem {
    Theme = 1,
    Save,
    Share,
    Download,
    Setting,
    NewSession,
    Upload,
    OpenCachedSession
  }

  